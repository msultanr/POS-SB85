-- Applied only after verifying the preceding copy preserves each image and version.
-- Image data remains in settings ('logo' / 'qris'); no images are deleted.
DROP TABLE `LogoSetting`;
DROP TABLE `QrisSetting`;
